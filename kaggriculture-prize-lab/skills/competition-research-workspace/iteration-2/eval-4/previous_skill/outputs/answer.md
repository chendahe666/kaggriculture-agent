季节基线只能说明相对基线的表现，不能证明N胜过M；重训N还混入了训练数据更新的影响。先冻结M、N、特征处理、决策阈值及H的完整月份评估规则，免费生成两者在H的预测，按相同信息可用时间、样本和MAE口径作配对比较。检查分时段、实体退步，并按时间块或实体依赖估计差异的不确定性；单月结果不能证明跨月份稳定。我的安排是保留唯一训练额度，先用这次免费确认决定保留M还是选择N，再训练选定方案到部署允许的最新日期。若差异不足以支持切换，就保留M。H一经查看便不能反复调参后继续充当确认集；最终重训版本也没有因此获得独立验证。若必须提前重训N，仍须保留旧M、旧N的H对照，分别报告方法差异与更新收益，不能声称它胜过同样更新的M。

独立智能体比赛不必等所有track结束。单个交易排序模块完成合法性、实际触发、完整回合官方收益、退步和运行时检查，再通过预留场景确认，即可建议提交该候选。若与其他模块争用资源或改变后续状态，需补相关交互及恢复验证；无需等待无关研究。用户只授权本地评估，所以建议可给，实际提交仍需明确授权。

搜索预算曲线设为0、32、128、256次模拟，对应总耗时约20、28、52、84毫秒；256为暂定上限，留16毫秒余量。先在开发场景记录官方收益随预算的变化，再冻结预算，例如128次，让旧排序与新排序在同样CPU、其余模块、对手版本、种子和席位安排下跑完整回合，以回合或种子席位块比较收益和不确定性，并用未参与选择的场景确认。还须核验真实最坏耗时、尾延迟、冷启动、内存、合法观测与模拟准确性；设计时截止及可执行回退。配对种子不保证两策略后续随机世界完全相同。

# Kaggriculture Agent 执行规划

> 规划快照：2026-09-02  
> 当前线上状态：V0 已通过 Kaggle validation，截图时技能评分为 226.9；该数值会随对局变化。  
> 当前本地基线：固定 seed `20260901`，V0 对官方 `starter` 为 `3961 : 3750`。

## 总目标

用 7 天建立一条可以持续复用的 Agent 开发流水线，而不是只做一次性的高分尝试：

```text
冻结基线 → 提出单一假设 → 实现最小改动 → 本地冒烟
→ 多 seed 配对 A/B → promote / hold / reject
→ 用户批准 → Kaggle validation / episodes → 复盘
```

## 全程硬门槛

任何候选版本只有同时满足以下要求，才可以进入下一阶段：

- 完整 720-step Episode 完成率 100%。
- 双方最终状态均为 `DONE`。
- 返回结构错误、未捕获异常、wrapper 异常均为 0。
- 没有明显超时。
- 与冻结基线使用相同 seeds、对手和双方位置进行配对比较。
- 报告最差结果和失败样本，不只报告最好结果。
- 每个实验最后明确给出 `promote`、`hold` 或 `reject`。

## 当前已完成

- [x] 建立独立可提交的 `main.py`。
- [x] 本地完整运行 720 步。
- [x] 通过 `main.py` 文件路径加载测试。
- [x] 固定 seed 下战胜官方 `starter`，`3961 : 3750`。
- [x] 上传 Kaggle 并通过 validation。
- [x] 建立运行报告、逐回合日志和中文开发手册。
- [x] 建立仓库级 `AGENTS.md` 开发纪律。

## Session 1：明早 60 分钟

目标：理解线上结果，确认本地基线，并批准 Day 1 的评估规格；这一小时不追求新分数。

### 0–10 分钟：保存线上证据

- 打开 Submissions，记录当前 status、score、submission ID 和 episode 数。
- 在线查看 Replay。
- 查看 Agent 0 / Agent 1 logs；若有内容则下载。
- 写下这是 validation self-play 还是普通匹配 Episode，避免混淆双方身份。

### 10–20 分钟：重跑冻结 V0

```powershell
Set-Location 'C:\Users\chend\OneDrive\文档\ChatGPT\kaggle-agriculture\kaggriculture-agent'
py -3.13 -B test_local.py
```

预期：720 steps、双方 `DONE`、异常为 0；固定环境中结果应与基线一致。

### 20–35 分钟：理解瓶颈

- V0 的 719 次调用中，农夫 `PASS` 667 次，占 92.77%。
- 农夫从未离开 `[4,4]`。
- 只使用 25 块初始土地中的 1 块。
- 终局仍有一个未成熟作物和一粒未用种子。
- 本地赢一局只证明接口和基本策略可用，不证明榜单竞争力。

### 35–50 分钟：批准 Day 1 评估设计

第一版建议：

- 10 个固定 seeds。
- 对手：`pass`、`random`、`starter`。
- 每个条件交换玩家 0/1 位置。
- 总计 60 局。
- 保存逐局 JSON/CSV 和汇总 Markdown。

必须记录：完成状态、双方现金、胜负、运行时间、异常、返回结构错误。

### 50–60 分钟：发出第一条开发任务

```text
按照 AGENTS.md 和 DEVELOPMENT_PLAN.md 执行 Phase 1。先冻结并测量 V0，
只建立多 seed、三种官方对手、双方换位的配对评估系统，不修改 main.py 策略。
输出逐局机器可读结果和汇总报告；通过 Phase 1 Gate 后停止，等待我批准。
```

## Phase 1 / Day 1：评估系统

### 单一目标

建立以后所有策略版本共用的测量工具，不改变 V0 决策。

### 任务

- [x] 保存 V0 commit/hash 和文件摘要。
- [x] 创建批量 benchmark runner。
- [x] 固定 seeds、对手和双方位置。
- [x] 输出逐局结构化结果。
- [x] 汇总胜率、现金中位数、最差值、运行时间和所有失败。
- [x] 确认评估不会修改 submission 文件。

### Phase 1 Gate

- [x] 60/60 Episodes 完整完成。
- [x] 所有异常和 shape-invalid 均为 0。
- [x] 重跑同一评估集能复现确定性部分。
- [x] 报告能够把不同对手、seed 和玩家位置分开显示。

Phase 1 于 2026-09-02 通过。`pass` 与 `starter` 的 40 个确定性条件在复跑中
逐字段一致；内置 `random` 对手的 20 局会自行产生随机漂移，因此只作为随机
压力测试，不作为精确回归判据。基线报告见
`benchmarks/v0_baseline_20260902/`，复跑见
`benchmarks/v0_baseline_repeat_20260902/`。

### 人机检查点 A

用户只需要决定：**这套评估集是否足以作为未来一周的统一尺子？**

**状态：2026-09-02 已批准。** Phase 1 Gate 通过后可进入 Phase 2。

## Phase 2 / Day 1：Endgame guard

### 单一假设

当剩余时间不足以让新作物成熟并出售时停止买种和种植，可以减少终局未兑现资产。

### 任务

- [x] 只修改终局购买/种植条件。
- [x] 不改变路线、作物、出售策略或工人逻辑。
- [x] 使用 Phase 1 完全相同的评估集对比 V0。
- [x] 记录终局未成熟作物、未用种子、携带物和 shed 库存。

### Phase 2 Gate

- [x] 所有硬门槛保持通过。
- [x] 固定原始轨迹验证“理论上约 +20 金币”的推论是否成立。
- [x] 多 seed 配对结果没有新的终局回退。

Phase 2 于 2026-09-02 通过。在 `pass / starter` 的全部 40 个确定性配对条件中，
V1 均严格比 V0 多 20 金币；终局未用种子从 1 降至 0，剩余未成熟 Wheat 从
1 块降至 0，且完成率、动作结构和异常门槛保持通过。`random` 只用于随机压力
测试，不把独立随机轨迹的逐局差值当作因果证据。详见
`reports/phase2_endgame_guard.md`。

## Phase 3 / Day 2：3–5 格任务调度

### 单一假设

主农夫使用小规模多格路线，可以显著减少纯等待，同时保持必要浇水完成率 100%。

### 任务

- [x] 扫描初始 NW 5×5 地块并选择固定的相邻管理区。
- [x] 生成 `WATER / HARVEST / PLANT / DIG / MOVE` 任务。
- [x] 按死亡风险、成熟衰减、可回本种植、杂草、移动排序。
- [x] 先实现 3 格并通过稳定性评估；5 格进入 Phase 4 规模实验。
- [x] 不加入 hand、动物、扩地或动态换作物。

### Phase 3 Gate

- [x] 漏浇造成的作物死亡为 0。
- [x] 能解释每个移动动作对应的任务。
- [x] PASS 比例下降，但不以无意义动作填充回合。
- [x] 配对现金结果稳定优于 Phase 2 候选。

Phase 3 于 2026-09-02 通过。V2 的 60/60 Episodes 全部完成且零错误、
plant-to-weed 转换为 0；现金中位数从 V1 的 3964 提升至 5879，最差现金
从 3799 提升至 5264。在 40 个确定性配对条件中，现金提升最小 1518、
中位数 1867.5、最大 2189，全部为正。详见
`reports/phase3_three_tile_scheduler.md`。

## Phase 4 / Day 3：规模选择与库存边界

### 单一目标

比较 1、3、5、10 格，用数据选择风险调整后最稳的规模。

### 任务

- [ ] 使用完全相同的 benchmark 条件比较四种规模。
- [ ] 记录成熟到收获延迟、每次收获动作数和有效地块数。
- [ ] 处理自然杂草、日界刷新、携带库存和自动入仓。
- [ ] 检查最后一天所有产品是否已经变成银行现金。

### Phase 4 Gate

- [ ] 选择一个规模并写明拒绝其他规模的证据。
- [ ] 不因单局最高现金选择波动更大的版本。

### 人机检查点 B

用户选择：继续优化多格路线，还是批准进入 farm-hand 实验。

**状态：2026-09-02 已预先批准。** 仍须先通过 Phase 3–4 Gate，不能跳过前置验证。

## Phase 5 / Day 4：Farm hands

### 单一假设

调度稳定后，1–2 个 hand 的边际收入可以覆盖雇佣和协调成本。

### 任务

- [ ] 比较 0、1、2 hands。
- [ ] 防止多个单位争抢相同任务。
- [ ] 记录 hand-day 净收益、移动、PASS 和冲突。
- [ ] 只有 1 hand 稳定赚钱时才测试 2 hands。

### Phase 5 Gate

- [ ] 多单位没有任务冲突或漏维护。
- [ ] 候选在中位数和最差结果上都有可接受收益。

## Phase 6 / Day 5：作物和市场

### 单一目标

在冻结调度器上逐项测试经济策略。

### 实验顺序

1. 纯 Wheat。
2. Wheat + Carrot。
3. 根据当前价格和 town shops 选择二者。
4. 立即出售 vs 简单阈值出售。

### 限制

- 本阶段不加入动物。
- 不同时更改作物选择和 hand 数量。
- 不使用未经验证的复杂价格预测。

### Phase 6 Gate

- [ ] 报告单位土地日毛利和单位动作毛利。
- [ ] 报告平均实现售价、库存滞留、shed 峰值和资金断裂。
- [ ] 只保留跨 seeds、对手和位置稳健的规则。

## Phase 7 / Day 6：稳健性

### 任务

- [ ] 对 `pass / random / starter / V0 / 前一稳定候选`。
- [ ] 扩大 seeds，并交换玩家位置。
- [ ] 检查不同 town shops、weed 事件和最差现金轨迹。
- [ ] 对最差三局定位第一个错误决策，而不是只看终局。
- [ ] 检查完整运行时间和最慢单回合。

### Phase 7 Gate

- [ ] 完成率 100%，异常和超时为 0。
- [ ] 没有只针对 `starter` 的明显过拟合。
- [ ] 选出最多两个最终候选。

## Phase 8 / Day 7：冻结与用户批准提交

### 本地最终检查

- [ ] `main.py` 自包含、无网络、无凭据、无绝对路径。
- [ ] `agent(obs)` 仍为最后一个顶层 callable。
- [ ] 文件路径完整 720-step 测试通过。
- [ ] 全部 benchmark 和回归测试通过。
- [ ] 保存候选 commit hash、结果报告和 submission description。
- [ ] 核对 Kaggle 当前最近两个 active submissions。

### 人机检查点 C

AI 提交一个简短决策包：

- 候选与线上 V0 的配对结果。
- 最差三局和剩余风险。
- 推荐提交的文件、commit 和描述。
- `promote / hold / reject` 建议。

**状态：2026-09-02 已给予持续批准。** 通过所有前置 Gate 的最终候选可以上传；
该批准持续到用户修改。每次上传后必须执行 `reports/submissions/README.md`
定义的人类侧报告协议。


## 暂不做事项

- 强化学习。
- 在线调用 LLM/API。
- 动物完整产业链。
- 土地扩张。
- 一次加入多个策略层。
- 仅根据当前技能评分 226.9 调参。

这些不是永远不做，而是在评估、调度和经济基础稳定前不做。
